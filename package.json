{
  "name": "gymcount",
  "version": "1.0.0",
  "description": "Aplicación para seguimiento de ejercicios",
  "main": "index.html",
  "scripts": {
    "build": "npm run copy-files",
    "copy-files": "mkdir -p www && cp index.html www/ && cp -r assets www/",
    "add-android": "npx cap add android",
    "sync": "npx cap sync",
    "open-android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0",
    "@capacitor/app": "^5.0.0",
    "@capacitor/storage": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
} 